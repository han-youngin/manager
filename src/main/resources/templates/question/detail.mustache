<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>
    <link rel="stylesheet" href="/css/common.css">
    <link rel="stylesheet" href="/css/post.css">
</head>

<body>
    <input type="hidden" value="{{question.id}}" id="questionId">
    <div class="d-flex justify-content-between">
        <div>
            <a href="/">
                <div>메인페이지로가기</div>
            </a>
        </div>
        <div>
            <a href="/s/question/list">
                <div>질문답변하러가기</div>
            </a>
        </div>
    </div>
    {{#question}}
    <ul class="list-group list-group-flush">
        <li>제목</li>
        <li class="list-group-item">{{title}}</li>
        <li>내용</li>
        <li class="list-group-item">{{content}}</li>
    </ul>
    {{/question}}
    {{#question.file}}
    <div class="full">
        <button id="btn-checkImg" class="full mt-3 btn btn-primary input-text">사진 확인하기</button>
    </div>
    {{/question.file}}
    <br />



    <!--답글-->
    <div class="card">
        <div class="card-header"><b>답변을 입력하세요</b></div>
        <form action="/s/question/{{question.id}}/list/comment" method="post" name="content">
            <div class="card-body">
                <textarea name="content" class="form-control" rows="1"></textarea>
            </div>
            <div class="card-footer">
                <button type="submit" class="btn btn-primary">등록</button>
            </div>
        </form>
    </div>
    <br />

    <div class="card">
        <div class="card-header"><b>답글</b></div>
        <ul id="reply-box" class="list-group">
            {{#question.IsComment}}
            <li class="list-group-item d-flex justify-content-between">
                <div>{{question.comment.content}}</div>
                <div class="d-flex">
                    <div class="font-italic">작성자 : 관리자 </div>
                </div>
            </li>
            {{/question.IsComment}}

        </ul>
    </div>
    <br />
    <script>
        $("#btn-checkImg").click(() => {
            imgCheck();
        });

        let imgCheck = () => {
            let questionId = $("#questionId").val();
            window.open(`/s/question/checkImg/${questionId}`, '이미지', 'location=no,status=no,scrollbars=yes');
        }
    </script>
</body>

</html>